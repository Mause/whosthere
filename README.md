# whosthere

TUI Application to discover devices on your local area network it's subnet.

## Configuration
Some configuration options are available via an configuration file as well as via command line arguments.
It is good to know that command line arguments always override configuration file options. On first run a default 
configuration file is created at `~/.config/whosthere/config.yaml`.